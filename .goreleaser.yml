version: 2

before:
  hooks:
    - go mod download

builds:
  - id: eru-core-debug
    binary: eru-core.dbg
    env:
      - CGO_ENABLED=0
    ldflags:
      - -X github.com/projecteru2/core/version.REVISION={{.Commit}}
      - -X github.com/projecteru2/core/version.VERSION={{.Env.VERSION}}
      - -X github.com/projecteru2/core/version.BUILTAT={{.Date}}
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    main: .

  - id: eru-core-release
    binary: eru-core
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s
      - -X github.com/projecteru2/core/version.REVISION={{.Commit}}
      - -X github.com/projecteru2/core/version.VERSION={{.Env.VERSION}}
      - -X github.com/projecteru2/core/version.BUILTAT={{.Date}}
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    main: .

archives:
  - id: release
    ids:
      - eru-core-release
    name_template: >-
      {{- .ProjectName }}_{{- .Version }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end -}}

  - id: debug
    ids:
      - eru-core-debug
    name_template: >-
      {{- .ProjectName }}_{{- .Version }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}_debug
      {{- if .Arm }}v{{ .Arm }}{{ end -}}

  # Unversioned alias so releases/latest/download/eru-core_Linux_x86_64.tar.gz resolves.
  - id: release-latest
    ids:
      - eru-core-release
    name_template: >-
      {{- .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end -}}

checksum:
  name_template: 'checksums.txt'

release:
  prerelease: auto

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
